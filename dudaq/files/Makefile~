APP = dudaq

# Add any other object files to this list below
APP_OBJS = dudaq.o chc.o buffer.o scope.o ad_shm.o

all: build

build: $(APP)

clean:
	rm *.o
	rm dudaq

$(APP): $(APP_OBJS)
	$(CC) $(LDFLAGS) -DSCOPE_V4 -DTRIG_INT -DFake -o $@ $(APP_OBJS) $(LDLIBS)

